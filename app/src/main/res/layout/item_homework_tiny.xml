<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <import type="com.xyy.simplehomework.entity.MySubject" />
        <variable
            name="homework"
            type="com.xyy.simplehomework.entity.Homework" />
    </data>
    <android.support.v7.widget.CardView
        android:id="@+id/homework"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginBottom="8dp"
        android:layout_marginRight="16dp"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        app:cardBackgroundColor="@{homework.subject.getTarget().color}"
        tools:cardBackgroundColor="@color/colorPrimary"
        app:cardElevation="0dp"
        android:layout_marginEnd="16dp">
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="@android:color/white"
            android:layout_marginLeft="10dp"
            android:layout_marginTop="6dp"
            tools:text="计算机网络"
            android:textStyle="bold"
            android:text="@{homework.subject.getTarget().getName()}"
            android:layout_marginStart="10dp" />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="@android:color/white"
            android:layout_gravity="bottom"
            android:layout_marginLeft="10dp"
            android:layout_marginBottom="8dp"
            tools:text="截止日期：10.2"
            android:text="@{homework.deadline}"
            android:layout_marginStart="10dp" />
    </android.support.v7.widget.CardView>
</layout>
